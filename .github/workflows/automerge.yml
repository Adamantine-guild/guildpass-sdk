name: Enable Auto-merge

on:
  pull_request_target:
    types: [opened, reopened, ready_for_review]

jobs:
  call-automerge:
    uses: Adamantine-guild/.github/.github/workflows/automerge-reusable.yml@main
    secrets: inherit